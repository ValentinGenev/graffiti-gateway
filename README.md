# Graffiti's API gateway
A simple API gateway for a message board

# Run locally
- run `docker-compose build`;
- run `docker-compose up`;

## Development
- change database host to `DB_HOST=localhost`; 